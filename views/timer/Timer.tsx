import React from 'react';
import {SafeAreaView, Text} from 'react-native';

const Timer = () => {
  return (
    <SafeAreaView>
      <Text style={{backgroundColor: 'black'}}>TIMER VIEW</Text>
    </SafeAreaView>
  );
};

export default Timer;
